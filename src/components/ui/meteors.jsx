"use client";
import { cn } from "@/lib/utils";

// DISABLED: This component causes diagonal lines on desktop due to rotate transform
export const Meteors = ({ number = 20 }) => {
  return null;
};

export default Meteors;


